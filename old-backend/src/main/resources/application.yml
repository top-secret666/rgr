server:
  port: 8080
  error:
    whitelabel:
      enabled: true

spring:
  sql:
    init:
      platform: mysql
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/loltournament?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: R3l14bl3$3cur1ty!
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
    generate-ddl: true
